{
  "name": "@iievi/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed fetch functions for the IIEVI API. Portable to React Native \u2014 uses only the fetch standard, no DOM or Next.js APIs.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@iievi/types": "workspace:*",
    "@iievi/validators": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
